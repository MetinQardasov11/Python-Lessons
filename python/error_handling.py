# Errors

# ! Keyerror

data = {'name' : 'Metin', 'age' : 20}
# print(data['city'])

# ---------------------------------------------------------------------------------------------------------------------------------------

# ! Indexerror
data = [1, 2, 3, 4]
# print(data[7])

# ---------------------------------------------------------------------------------------------------------------------------------------

# ! Nameerror
# print(name)

# ---------------------------------------------------------------------------------------------------------------------------------------

# ! TypeError
a, b = 5, '5'
# print(a + b)

# ---------------------------------------------------------------------------------------------------------------------------------------

# ! ValueError
# a = int('metin')
# print(a)

# ---------------------------------------------------------------------------------------------------------------------------------------

# ! ZeroDivisionError
# print(5 / 0)

# ---------------------------------------------------------------------------------------------------------------------------------------

# ! SyntaxError
# if = 5
# print(if)

# ---------------------------------------------------------------------------------------------------------------------------------------

'''

try:
    # try code
except:
    # catch error
else:
    # continue after try
finally:
    # always run code

'''


# ---------------------------------------------------------------------------------------------------------------------------------------

try:
    print(6 / 0)
except:
    print('ZeroDivisionError')
    
print('DONE!')

# ---------------------------------------------------------------------------------------------------------------------------------------

a = int(input('A: ').strip())
b = int(input('B: ').strip())

try:
    print(a / b)
except:
    print('Error with zero!')

# ---------------------------------------------------------------------------------------------------------------------------------------

a = input('A: ').strip()
b = input('B: ').strip()

try:
    print(int(a) / int(b))
except Exception as e:
    print(f'Some problem: {e}')

# ---------------------------------------------------------------------------------------------------------------------------------------

a = input('A: ').strip()
b = input('B: ').strip()

try:
    result = int(a) / int(b)
    print(result)
except Exception as e:
    print(f'Error: {e}')
else:
    result *= 100
    print(f'new result:',result)
finally:
    print('Done!')

# ---------------------------------------------------------------------------------------------------------------------------------------

def division(a, b):
    if b == 0:
        raise Exception('Zero error')
    if type(a) != int or type(b) != int:
        raise Exception('Type error')    
    return a / b

try:
    print(division(2, 'text'))
except Exception as e:
    print(f'Error : {e}')

# ---------------------------------------------------------------------------------------------------------------------------------------

def division(a, b):
    assert type(a) == int and type(b) == int, 'Type error'
    return a / b

try:
    print(division(10, '5'))
except Exception as e:
    print(f'Error : {e}')

# ---------------------------------------------------------------------------------------------------------------------------------------

def read_file(file_name):
    try:
        with open(file_name, 'r') as file:
            return file.read()
    except FileNotFoundError:
        return f'File {file_name} not found!'
    except Exception as e:
        return f'Error: {e}'

print(read_file('file.txt'))